class Solution {
public:
    int gcd(int &x, int y)
    {
        if(x < y) swap(x, y);

        if(y == 0) return x;
        return gcd(y, x%y);
    }
    bool canMeasureWater(int x, int y, int z) {
        return z == 0 or ((z <= (x + y)) and ((z % gcd(x, y)) == 0));
    }
};
